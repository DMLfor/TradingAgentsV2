# TradingAgentsV2 Environment Variables
# Copy to .env and fill in your actual values (not tracked by git)

# MySQL password (overrides tdx_config.yaml)
MYSQL_PASSWORD=your_password

# Cloud SQLite path override (optional)
# TDX_DB_PATH=data/tdx_data_cloud.db
